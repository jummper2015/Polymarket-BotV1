# Fail2ban filter — Polymarket dashboard login failures.
#
# Matches the warning emitted by bot/dashboard.py:248 on a wrong password.
# The remote address appears in the message and is the value fail2ban bans.

[Definition]
failregex = \[dashboard\] intento de acceso fallido desde <HOST>
ignoreregex =
